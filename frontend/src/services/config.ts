const API = process.env.EXPO_PUBLIC_API_URL;

export const testUrl = `${API}/test`;
export const transactionsUrl = `${API}/transactions`;
export const categoriesUrl = `${API}/categories`;
export const aiUrl = `${API}/insight`;